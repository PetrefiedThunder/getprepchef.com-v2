# Environment Configuration for PrepChef
#
# QUICK START FOR DEMOS:
# This file contains MOCK API keys and placeholder credentials for running demos.
# Copy this file to .env to get started immediately:
#   cp .env.example .env
#
# IMPORTANT FOR PRODUCTION:
# - Replace all JWT secrets with secure random values (use: openssl rand -hex 32)
# - Replace webhook signing secret with a secure random value
# - Add real API keys for external services (SendGrid, Twilio, S3)
# - Change STORAGE_TYPE to 's3' and configure real S3 credentials
# - Set NODE_ENV to 'production'
#
# Copy this file to .env and update with your values

# Node Environment
NODE_ENV=development

# Server
PORT=3000
HOST=0.0.0.0

# Database
MONGODB_URI=mongodb://mongodb:27017/prepchef
MONGODB_MAX_POOL_SIZE=10

# Redis (for BullMQ)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
# Generate with: openssl rand -hex 32
# DEMO VALUES - Replace in production!
JWT_ACCESS_SECRET=demo-jwt-access-secret-f8d9a7e6c5b4a3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a7b6c5d4e3f2a1b0c9
JWT_REFRESH_SECRET=demo-jwt-refresh-secret-a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# CORS
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# File Storage
# Use 'local' for demo/development, 's3' for production
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./uploads
# S3 Configuration (MOCK values for demos - replace in production!)
# To use S3, set STORAGE_TYPE=s3 and configure these values
S3_BUCKET=prepchef-demo-bucket
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=AKIAMOCKDEMOKEY12345678
S3_SECRET_ACCESS_KEY=mockSecretAccessKeyForDemoPurposesOnly1234567890abcdefghij

# Webhook
# DEMO VALUE - Replace in production!
WEBHOOK_SIGNING_SECRET=demo-webhook-signing-secret-a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4
WEBHOOK_TIMEOUT_MS=5000
WEBHOOK_MAX_RETRIES=3

# External Services (optional for MVP)
# These are MOCK values for demos - replace with real keys for production!
# To use real services, uncomment and add your actual API keys
SENDGRID_API_KEY=SG.mock-sendgrid-api-key-for-demo-purposes-only-replace-in-production
TWILIO_ACCOUNT_SID=ACmock1234567890abcdef1234567890ab
TWILIO_AUTH_TOKEN=mock_twilio_auth_token_32_chars_demo_only

# Logging
LOG_LEVEL=info
LOG_PRETTY=true

# Security
BCRYPT_ROUNDS=12

# Background Jobs
VERIFICATION_JOB_CONCURRENCY=5
REG_CLEARINGHOUSE_CRON=0 8,12,20 * * *

# Feature Flags
ENABLE_EMAIL_NOTIFICATIONS=false
ENABLE_SMS_NOTIFICATIONS=false
